---
'cli': minor
---

Added `deadrop vault list` to show every vault in the config, marking the active one. `deadrop vault use` now prompts you to pick from that list when you run it without a name, instead of failing.
